{
  "scripts": {
    "start": "elm-live --open --before-build ./before-build.sh -- src/Main.elm --output build/app.js"
  },
  "devDependencies": {
    "elm": "^0.19.0-bugfix6",
    "elm-live": "^3.4.0"
  }
}
